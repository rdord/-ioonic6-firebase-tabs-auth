import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ioonic6-firebase-tabs-auth',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
